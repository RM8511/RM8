-- RM8 GOLD ELITE: Westbound (Black & Gold Edition)
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()

-- تخصيص الألوان (الأسود والذهبي)
local colors = {
    SchemeColor = Color3.fromRGB(212, 175, 55), -- ذهبي
    Background = Color3.fromRGB(0, 0, 0), -- أسود مطفي
    Header = Color3.fromRGB(15, 15, 15), -- أسود خفيف للهيدر
    TextColor = Color3.fromRGB(255, 255, 255), -- نص أبيض
    ElementColor = Color3.fromRGB(20, 20, 20) -- خلفية الأزرار
}

local Window = Library.CreateLib("RM8 SYSTEM - Westbound", colors)

-- [[ إنشاء الزر العائم الذهبي ]] --
local ScreenGui = Instance.new("ScreenGui")
local ToggleButton = Instance.new("TextButton")
local Corner = Instance.new("UICorner")
local Stroke = Instance.new("UIStroke")

ScreenGui.Parent = game:GetService("CoreGui")
ToggleButton.Parent = ScreenGui
ToggleButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
ToggleButton.Position = UDim2.new(0.05, 0, 0.4, 0)
ToggleButton.Size = UDim2.new(0, 60, 0, 35)
ToggleButton.Text = "RM8"
ToggleButton.TextColor3 = Color3.fromRGB(212, 175, 55) -- نص ذهبي
ToggleButton.TextSize = 16
ToggleButton.Font = Enum.Font.GothamBold
ToggleButton.Draggable = true
ToggleButton.Active = true

Corner.CornerRadius = UDim.new(0, 8)
Corner.Parent = ToggleButton

Stroke.Parent = ToggleButton
Stroke.Color = Color3.fromRGB(212, 175, 55) -- إطار ذهبي
Stroke.Thickness = 2

ToggleButton.MouseButton1Click:Connect(function()
    Library:ToggleUI()
end)

-- [[ إعدادات RM8 ]] --
local RM8_Config = {
    SilentAim = false,
    AimAssist = false,
    Aimbot = false,
    TargetPart = "HumanoidRootPart",
    FOV = 150
}

local MainTab = Window:NewTab("القتال (Combat)")
local CombatSection = MainTab:NewSection("أنظمة الإيم - RM8")

CombatSection:NewToggle("تفعيل Silent Aim", "الرصاص ينجذب للخصم تلقائياً", function(state)
    RM8_Config.SilentAim = state
end)

CombatSection:NewToggle("تفعيل Aim Assist", "مساعدة في التصويب بنعومة", function(state)
    RM8_Config.AimAssist = state
end)

CombatSection:NewToggle("تفعيل Aimbot", "توجيه الكاميرا (عند الزووم)", function(state)
    RM8_Config.Aimbot = state
end)

local SettingsTab = Window:NewTab("الإعدادات")
local SettingsSection = SettingsTab:NewSection("تخصيص نظام RM8")

SettingsSection:NewDropDown("مكان الضرب", "حدد الهدف", {"Head", "HumanoidRootPart"}, function(option)
    RM8_Config.TargetPart = option
end)

SettingsSection:NewSlider("نطاق البحث (FOV)", "المسافة", 500, 50, function(v)
    RM8_Config.FOV = v
end)

-- [[ المحرك التقني لـ RM8 ]] --
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()
local Camera = game.Workspace.CurrentCamera

local function GetClosestTarget()
    local Target = nil
    local ClosestMag = RM8_Config.FOV
    for _, v in pairs(Players:GetPlayers()) do
        if v ~= LocalPlayer and v.Character and v.Character:FindFirstChild(RM8_Config.TargetPart) then
            local Hum = v.Character:FindFirstChild("Humanoid")
            if Hum and Hum.Health > 0 then
                local ScreenPoint, OnScreen = Camera:WorldToViewportPoint(v.Character[RM8_Config.TargetPart].Position)
                if OnScreen then
                    local Mag = (Vector2.new(ScreenPoint.X, ScreenPoint.Y) - Vector2.new(Mouse.X, Mouse.Y)).Magnitude
                    if Mag < ClosestMag then
                        Target = v
                        ClosestMag = Mag
                    end
                end
            end
        end
    end
    return Target
end

game:GetService("RunService").RenderStepped:Connect(function()
    local Target = GetClosestTarget()
    if Target and Target.Character and (RM8_Config.Aimbot or RM8_Config.AimAssist) then
        if game:GetService("UserInputService"):IsMouseButtonPressed(Enum.UserInputType.MouseButton2) then
            local Smoothness = RM8_Config.Aimbot and 0.15 or 0.05
            Camera.CFrame = Camera.CFrame:Lerp(CFrame.new(Camera.CFrame.Position, Target.Character[RM8_Config.TargetPart].Position), Smoothness)
        end
    end
end)

local OldNamecall
OldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
    local Args = {...}
    local Method = getnamecallmethod()
    if RM8_Config.SilentAim and Method == "FindPartOnRayWithIgnoreList" then
        local T = GetClosestTarget()
        if T then
            Args[1] = Ray.new(Camera.CFrame.Position, (T.Character[RM8_Config.TargetPart].Position - Camera.CFrame.Position).Unit * 1000)
            return OldNamecall(self, unpack(Args))
        end
    end
    return OldNamecall(self, ...)
end)
