-- RM8 GOLD ELITE: Westbound (Independent FOV Controls)
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()

local colors = {
    SchemeColor = Color3.fromRGB(212, 175, 55), -- ذهبي
    Background = Color3.fromRGB(0, 0, 0), -- أسود
    Header = Color3.fromRGB(15, 15, 15),
    TextColor = Color3.fromRGB(255, 255, 255),
    ElementColor = Color3.fromRGB(20, 20, 20)
}

local Window = Library.CreateLib("RM8 SYSTEM - Westbound", colors)

-- [ الزر العائم الذهبي ] --
local ScreenGui = Instance.new("ScreenGui", game:GetService("CoreGui"))
local ToggleButton = Instance.new("TextButton", ScreenGui)
local Corner = Instance.new("UICorner", ToggleButton)
local Stroke = Instance.new("UIStroke", ToggleButton)

ToggleButton.Size = UDim2.new(0, 60, 0, 35)
ToggleButton.Position = UDim2.new(0.05, 0, 0.4, 0)
ToggleButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
ToggleButton.Text = "RM8"
ToggleButton.TextColor3 = Color3.fromRGB(212, 175, 55)
ToggleButton.Draggable = true
ToggleButton.Active = true
Corner.CornerRadius = UDim.new(0, 8)
Stroke.Color = Color3.fromRGB(212, 175, 55)
Stroke.Thickness = 2

ToggleButton.MouseButton1Click:Connect(function() Library:ToggleUI() end)

-- [ الإعدادات الافتراضية ] --
local RM8_Config = {
    SilentAim = false,
    SilentFOV = 200,
    Aimbot = false,
    AimbotFOV = 150,
    TargetPart = "Head"
}

-- [ قائمة القتال ] --
local MainTab = Window:NewTab("القتال (Combat)")

-- قسم Silent Aim
local SilentSection = MainTab:NewSection("نظام Silent Aim")
SilentSection:NewToggle("تفعيل Silent Aim", "توجيه الرصاص تلقائياً", function(state) 
    RM8_Config.SilentAim = state 
end)
SilentSection:NewSlider("نطاق Silent FOV", "مسافة انجذاب الرصاص", 800, 50, function(v) 
    RM8_Config.SilentFOV = v 
end)

-- قسم Aimbot
local AimSection = MainTab:NewSection("نظام Aimbot")
AimSection:NewToggle("تفعيل Aimbot", "سحب الكاميرا للخصم", function(state) 
    RM8_Config.Aimbot = state 
end)
AimSection:NewSlider("نطاق Aimbot FOV", "مسافة سحب الكاميرا", 500, 50, function(v) 
    RM8_Config.AimbotFOV = v 
end)

-- قسم الإعدادات العامة
local SettingsTab = Window:NewTab("الإعدادات")
local GlobalSection = SettingsTab:NewSection("تخصيص الهدف")
GlobalSection:NewDropDown("مكان الضرب", "حدد الهدف", {"Head", "HumanoidRootPart"}, function(option)
    RM8_Config.TargetPart = option
end)

-- [[ محرك RM8 الذكي ]] --
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()
local Camera = game.Workspace.CurrentCamera

local function GetTarget(MaxDist)
    local Closest = nil
    local DistLimit = MaxDist
    for _, v in pairs(Players:GetPlayers()) do
        if v ~= LocalPlayer and v.Character and v.Character:FindFirstChild(RM8_Config.TargetPart) then
            local Hum = v.Character:FindFirstChild("Humanoid")
            if Hum and Hum.Health > 0 then
                local Pos, OnScreen = Camera:WorldToViewportPoint(v.Character[RM8_Config.TargetPart].Position)
                if OnScreen then
                    local Mag = (Vector2.new(Pos.X, Pos.Y) - Vector2.new(Mouse.X, Mouse.Y)).Magnitude
                    if Mag < DistLimit then
                        Closest = v
                        DistLimit = Mag
                    end
                end
            end
        end
    end
    return Closest
end

-- تفعيل الـ Silent Aim (برمجة الماوس)
local OldIndex
OldIndex = hookmetamethod(game, "__index", function(self, index)
    if self == Mouse and (index == "Hit" or index == "Target") and RM8_Config.SilentAim then
        local Target = GetTarget(RM8_Config.SilentFOV)
        if Target and Target.Character and Target.Character:FindFirstChild(RM8_Config.TargetPart) then
            return (index == "Hit" and Target.Character[RM8_Config.TargetPart].CFrame or Target.Character[RM8_Config.TargetPart])
        end
    end
    return OldIndex(self, index)
end)

-- تفعيل الـ Aimbot (حركة الكاميرا)
game:GetService("RunService").RenderStepped:Connect(function()
    if RM8_Config.Aimbot and game:GetService("UserInputService"):IsMouseButtonPressed(Enum.UserInputType.MouseButton2) then
        local T = GetTarget(RM8_Config.AimbotFOV)
        if T then
            Camera.CFrame = Camera.CFrame:Lerp(CFrame.new(Camera.CFrame.Position, T.Character[RM8_Config.TargetPart].Position), 0.1)
        end
    end
end)
